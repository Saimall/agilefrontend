const initialValue = {};

const emergingReducer = (state = initialValue, action) => {
  switch (action.type) {
    case value:

    default:
      return state;
  }
};

export default emergingReducer;
